# Agent 規則

1. 一開始會先規劃想做的內容，每次規劃完之後會先在對應文件的正確位置更新文件，我明確指示 agent 開始改檔案之前，不可以擅自開始修改任何檔案
2. 文件做根因修復，用正確邏輯直接覆蓋，不要打補丁，禁止寫否定的否定，直接寫肯定性的描述。 過時的敘述要清除乾淨。除非明顯需要用條列式呈現，不然不要一小句就斷行，要寫完整的段落再斷行
3. 程式碼做根因修復，用正確邏輯直接重構或覆蓋原有實作，保持程式結構一致且清晰。移除過時、冗餘與已失效的邏輯，確保最終程式碼只保留目前正確且有效的實作，不需要過度防禦
4. 不做需求範圍外的修改。
5. 不建立與現有功能重複的平行系統。
6. 遇到產品行為不明確時，不自行猜測並實作。
7. 遊戲規則文件一律以 `doc/spec.md` 為入口；拆分新的規格文件時，必須從 `doc/spec.md` 導流。如果認為一個主題適合開一個新的md檔，就開一個新的
8. `AGENTS.md` 只記錄協作規則與資料夾層級，不放遊戲規則
9. 永遠不要自動啟動瀏覽器和執行任何破壞性的命令
10. 無論壓縮多少次上下文，本AGENTS檔案永遠是最高層級規範

# 資料夾層級

- 專案根目錄：專案說明、協作規則、套件與 TypeScript、Vite、ESLint 等工具設定。
- `doc/`：遊戲設計與技術規格文件，入口為 `doc/spec.md`。
- `src/`：遊戲與介面原始碼。
- `src/components`：react UI 元件。
- `src/configs`：遊戲所有數值設定相關。
- `src/service`：遊戲邏輯。
- `src/state` xState 狀態機相關檔案。
- `src/store`：zustand 狀態相關檔案，react 跨元件溝通可用。
- `src/assets/`：由原始碼匯入的圖片等資源。
- `public/`：直接由網站根路徑提供的靜態資源。

# 每個檔案必須

1. 只有唯一職責，不可以混合職能
2. 壓在500行以內
3. 歸屬於自己適合的資料夾內，不可以一個 service 資料夾散落一堆為了同樣功能而寫的檔案，此時需要開一個資料夾放在 service 內，然後把相關檔案都歸入
4. 使用 typescript / scss / tailwind
5. react元件不要全塞一個檔案內，要適度拆分
